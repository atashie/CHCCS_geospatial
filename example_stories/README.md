# Example Stories

Editorial-style scrollytelling pages that compare specific schools using the same data and methodology as the neutral analysis pages in `src/`.

These are **editorial** (they make arguments based on the data) as opposed to the methodology walkthroughs in `src/` which are **neutral** (they explain how the analysis works).

## Environmental Conditions

**File:** `environmental_conditions_story.py`
**Output:** `environmental_conditions.html`

The first in an Ephesus-focused editorial series. Presents objective environmental/geographic data for all 11 elementary schools, making the case that environmental factors should inform closure decisions. Precedes the demographics story.

Three environmental dimensions:

1. **Flood Risk** — FEMA flood zone overlap with school properties (only FPG and Rashkis affected)
2. **Traffic Air Pollution (TRAP)** — modeled exposure from nearby roads with tree canopy mitigation
3. **Urban Heat Island (UHI)** — heat vulnerability proxy from ESA WorldCover land cover

### Color Scheme

- **Ephesus** = red (`#C62828`)
- **Glenwood** = green (`#2E7D32`)
- **Seawell** = blue (`#1565C0`)
- **FPG** = amber (`#FF8F00`)
- **Other schools** = gray (`#888888`)

### Usage

```bash
python example_stories/environmental_conditions_story.py
python example_stories/environmental_conditions_story.py --cache-only
```

### Prerequisites

| File | Generated By |
|------|-------------|
| `data/cache/nces_school_locations.csv` | `python src/road_pollution.py` |
| `data/cache/chccs_district_boundary.gpkg` | `python src/school_desert.py` |
| `data/cache/fema_flood_zones.gpkg` | `python src/flood_map.py` |
| `data/cache/trap_grids.npz` | `python src/environmental_map.py` |
| `data/cache/uhi_grid.npz` | `python src/environmental_map.py` |
| `data/processed/road_pollution_scores.csv` | `python src/road_pollution.py` |
| `data/processed/uhi_proxy_scores.csv` | `python src/environmental_map.py` |
| `data/raw/properties/combined_data_polys.gpkg` | `python src/property_data.py` |

### Architecture

- 45% narrative (left, scrollable) / 55% Leaflet map (right, fixed)
- Scrollama 3.2.0 + Leaflet 1.9.3 with CARTO light basemap
- TRAP and UHI raster grids rendered as base64 PNG image overlays
- Pure HTML/CSS bar charts for school comparisons
- FEMA flood zones as GeoJSON vector layers
- All data embedded as JS variables in a single self-contained HTML file

---

## CHCCS Demographics

**File:** `chccs_demographics_story.py`
**Output:** `chccs_demographics.html`

A data-driven comparison of Ephesus Elementary and Seawell Elementary across five dimensions:

1. **Socioeconomic Status** — poverty counts, affordable housing, median household income (drive-time vs. attendance-zone)
2. **Race/Ethnicity** — dot-density mapping, minority counts, Hispanic population
3. **Age Distribution** — young children (0–4) choropleth and bar charts, future enrollment demand
4. **Real Estate** — MLS home sales counts, median prices by zone (2023–2025)
5. **Planned Growth** — planned residential developments by nearest-drive zone, Ephesus vs. Seawell concentration

### Color Scheme

- **Ephesus** = red (`#C62828`)
- **Seawell** = blue (`#1565C0`)
- **Combined/actual values** = purple (`#6A1B9A`)
- **Other schools** = gray (`#cccccc`)

### Narrative Structure

The story opens with **methodology first** (steps 0–3): introducing the district, explaining attendance zones vs. drive-time zones, then focusing on Ephesus and Seawell. This ensures readers understand the zone concepts before seeing school comparisons.

All comparisons lead with **counts over percentages** — real human impact matters more than rates when zone populations differ significantly. Seawell's attendance zone is geographically large but sparsely populated in its outer reaches.

**Drive-time zones are the primary lens** since any closure triggers rezoning. Attendance zones are shown as dashed overlays for comparison.

### Key Analytical Insight

Seawell's attendance zone appears economically vulnerable, but drive-time analysis reveals much of that vulnerable population is actually closer to other schools. Meanwhile, Ephesus serves a larger, more diverse population with more young children and more affordable housing units. The SES summary slide shows median household income for both drive-zone and attendance-zone definitions, highlighting how boundary choice affects which block groups are captured.

### Usage

```bash
# Generate the HTML (requires cached data from prior analyses)
python example_stories/chccs_demographics_story.py

# Same behavior — all data is cache-only
python example_stories/chccs_demographics_story.py --cache-only
```

### Prerequisites

The following cached data files must exist (generated by other analysis scripts):

| File | Generated By |
|------|-------------|
| `data/cache/nces_school_locations.csv` | `python src/road_pollution.py` |
| `data/cache/chccs_district_boundary.gpkg` | `python src/school_desert.py` |
| `data/raw/properties/CHCCS/CHCCS.shp` | Manual (CHCCS shapefile) |
| `data/cache/census_acs_blockgroups.gpkg` | `python src/school_socioeconomic_analysis.py` |
| `data/cache/census_decennial_blocks.gpkg` | `python src/school_socioeconomic_analysis.py` |
| `data/raw/properties/combined_data_polys.gpkg` | `python src/property_data.py` |
| `data/cache/affordable_housing.gpkg` | `python src/affordable_housing.py` |
| `data/processed/census_school_demographics.csv` | `python src/school_socioeconomic_analysis.py` |
| `data/cache/mls_home_sales.gpkg` | `python src/mls_geocode.py` (optional — real estate slides degrade gracefully) |
| `data/cache/planned_developments.gpkg` | `python src/planned_dev_geocode.py` (optional — CH Active Dev slide degrades gracefully) |
| `data/raw/properties/planned/SAPFOTAC_2025_future_residential.csv` | Committed (optional — SAPFOTAC slide degrades gracefully) |
| `data/processed/school_desert_grid.csv` | `python src/school_desert.py` (optional — drive-time comparison degrades gracefully) |

### Architecture

Same two-column scrollytelling pattern as `src/socioeconomic_story.py`:
- 45% narrative (left, scrollable) / 55% Leaflet map (right, fixed)
- Scrollama 3.2.0 step triggers
- Leaflet 1.9.3 with CARTO light basemap
- Canvas-rendered dot-density layer (~95K dots)
- MLS home sales, CH Active Dev, and SAPFOTAC planned development point layers on Leaflet map (drive zones + dots/markers)
- Planned dev markers (both datasets) use blue→yellow→red gradient by unit count, matching the methodology map symbology
- Ephesus-vs-Seawell comparison tables with Drive Zone / Attendance Zone rows for select metrics
- Planned dev split into slide 16a (CH Active Dev units) and 16b (SAPFOTAC elementary student yields), with provenance explanation
- Pure HTML/CSS bar charts for earlier slides (no external charting library)
- All data embedded as JS variables in the HTML (including `var MLS`, `var DEV`, and `var SAPFOTAC` GeoJSON)
- Self-contained single HTML file (typically 5–8 MB)

---

## School Closure Scenarios

**File:** `closure_scenarios_story.py`
**Output:** `closure_scenarios.html`

Third and final editorial story. Examines student movement under closure scenarios — traffic redistribution, transportation costs, and the school desert risk of closing eastern schools.

Eleven scrollytelling steps (slide numbers 1, 2, 3, 3b, 4, 4b, 5, 5b, 6, 7, 8):

1. **Introduction** — Transportation costs as a closure criterion; maps visualize current enrollment and capacity for absorption under closure scenarios
2. **Capacity Overview** — District-wide 2030 projected enrollment vs. capacity with per-school map labels; rationale for 2030 figures, Seawell fewest displaced students, LEAP controlled-enrollment context
3. **Seawell Closure: Traffic (5–9)** — Traffic delta visualization for current students
3b. **Seawell Closure: Traffic (0–4)** — Same for pre-K children
4. **Ephesus Closure: Traffic (5–9)** — More road segments affected
4b. **Ephesus Closure: Traffic (0–4)** — 0-4 age disparity
5. **Young Children Bar Chart** — Children under 5 by nearest-drive school
5b. **Enrollment Projections** — Projected 2030 utilization bar chart (PMR2 Forecast)
6. **Where the Children Live** — Block group choropleth of children 5-9
7. **School Desert Scenario** — Close both Ephesus + Glenwood; school desert in densest area
8. **Summary** — Four key findings with links to other stories

### Color Scheme

- **Ephesus** = red (`#C62828`)
- **Glenwood** = green (`#2E7D32`)
- **Seawell** = blue (`#1565C0`)
- **FPG** = amber (`#FF8F00`)
- **Other schools** = gray (`#888888`)

### Usage

```bash
python example_stories/closure_scenarios_story.py
python example_stories/closure_scenarios_story.py --cache-only
```

### Prerequisites

| File | Generated By |
|------|-------------|
| `data/cache/nces_school_locations.csv` | `python src/road_pollution.py` |
| `data/cache/chccs_district_boundary.gpkg` | `python src/school_desert.py` |
| `data/cache/network_drive.graphml` | `python src/school_closure_analysis.py` |
| `data/processed/school_closure_traffic.csv` | `python src/school_closure_analysis.py` |
| `data/processed/school_closure_assignments.csv` | `python src/school_closure_analysis.py` |
| `data/cache/closure_analysis/pixel_children.csv` | `python src/school_closure_analysis.py` |
| `data/cache/tiger_blockgroups_orange.gpkg` | `python src/school_closure_analysis.py` |
| `data/cache/closure_analysis/children_blockgroups.csv` | `python src/school_closure_analysis.py` |
| `data/raw/properties/combined_data_polys.gpkg` | `python src/property_data.py` |

### Architecture

Same two-column scrollytelling pattern as the other editorial stories:
- 45% narrative (left, scrollable) / 55% Leaflet map (right, fixed)
- Scrollama 3.2.0 + Leaflet 1.9.3 with CARTO light basemap
- Full road network colored by traffic delta (diverging red/blue scale)
- Block group choropleth for children density
- DivIcon capacity labels with per-school directional offsets showing enrollment/capacity/% occupied; center-aligned text; highlighted schools use `SCHOOL_COLORS`, others use utilization-based coloring
- Dashed-circle markers for closed schools
- Pure HTML/CSS bar charts for school comparisons (children 0-4, enrollment utilization)
- All data embedded as JS variables in a single self-contained HTML file
